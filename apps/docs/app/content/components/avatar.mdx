---
title: Avatar
description: An image element with a fallback for representing the user.
references:
  radix:
    link: https://www.radix-ui.com/docs/primitives/components/avatar
    api: https://www.radix-ui.com/docs/primitives/components/avatar#api-reference
---
<ContentContainer>

<Section gap="1">
# Avatar
<Paragraph size={'extraLarge'} color={'tertiary'}>An image element with a fallback for representing the user.</Paragraph>
<ComponentPreview name="avatar" withRecipe />
</Section>

<Section>
## Installation

<Steps>
<Section>
### Install the following dependencies

<InstallTabs pkg="@radix-ui/react-avatar" />

### Copy and paste the following code into your project

<ComponentSource name="avatar" />

### Update the import paths to match your project setup
</Section>
</Steps>

</Section>

<Section>
## Usage

```tsx
import Avatar from '@/components/ui/avatar'

<Avatar.Root size="md" shape="circle">
  <Avatar.Image src="https://github.com/nanopx.png" alt="@nanopx" />
  <Avatar.Fallback>NP</Avatar.Fallback>
</Avatar.Root>
```
</Section>

<Section>
## Examples

### Shapes
The Avatar component supports two shapes: `circle` (default) and `square`.

<ComponentPreview name="avatar" file="shapes" />

### Sizes
Available sizes: `sm` (32px), `md` (40px), `lg` (48px), and `xl` (56px). Default is `md`.

<ComponentPreview name="avatar" file="sizes" />

### Fallbacks
The fallback is displayed when the image fails to load or when no image is provided.

<ComponentPreview name="avatar" file="fallbacks" />

</Section>

<Section>
## API Reference

### Root Props
| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| size | `'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | Controls the size of the avatar |
| shape | `'circle' \| 'square'` | `'circle'` | Controls the shape of the avatar |

</Section>

</ContentContainer>